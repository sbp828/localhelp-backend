[Unit]
Description=LocalHelp Backend Service
After=network.target

[Service]
User=localhelp
WorkingDirectory=/app
ExecStart=/usr/bin/java -jar /app/target/app.jar
Environment=DB_HOST="db-dev.localhelp.store"
Restart=always

[Install]
WantedBy=multi-user.target
EOF
